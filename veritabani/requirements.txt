# veritabani/ pipeline bağımlılıkları.
#
# Bu dosya 2026-07-31'de o günkü venv'in TAM dökümüydü (38 paket) — depolar
# birleşirken venv taşınamadığı için aceleyle `pip freeze` ile alınmıştı. Aynı gün
# düzeltildi: aşağıdakiler scripts/ altındaki kodun GERÇEKTEN import ettiği
# paketlerdir (ölçüldü). Geri kalanı ya bunların bağımlılığıydı (numpy, et_xmlfile,
# python-dateutil…) ya da yalnızca arşivdeki notebook'a aitti (aşağıya bakın).
#
# Kurulum:  python3 -m venv venv && venv/bin/pip install -r requirements.txt
# Bu venv web/venv ile KARIŞTIRILMAMALI (biri pandas pipeline'ı, diğeri Django).

pandas==3.0.3            # tüm temizleme/normalizasyon hattı (numpy'ı da getirir)
openpyxl==3.1.5          # Excel okuma/yazma + gömülü görsellerin OOXML çözümlemesi
psycopg2-binary==2.9.12  # scripts/database/ -> Postgres
pillow==12.3.0           # scripts/images/ -> küçük resim üretimi, görsel doğrulama

# archive/notebooks/migration.ipynb için EK olarak sqlalchemy ve bir Jupyter
# çekirdeği gerekiyor; o notebook aktif hattın parçası değil (archive/ gitignored),
# bu yüzden buraya konulmadı. Gerekirse:
#   venv/bin/pip install sqlalchemy==2.0.51 ipykernel
