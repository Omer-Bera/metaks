# Python
venv/
__pycache__/
*.pyc

# Django
# 2026-07-31'de 'default' bağlantısı Postgres'e (metaks_web) taşındı; kalan
# db.sqlite3.yedek-* dosyaları taşıma öncesi anlık görüntüdür — parola hash'i
# içerdikleri için bunlar da git'e girmemeli.
db.sqlite3*
staticfiles/
media/

# Ortam değişkenleri / sırlar
.env

# Tip denetleyici yerel ayarı: içeriği makineye özgü yorumlayıcı yolu
# (python_interpreter = "venv/bin/python"), paylaşılan bir proje ayarı değil.
pyrefly.toml

# macOS
.DS_Store
